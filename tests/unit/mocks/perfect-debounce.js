// CJS stub for perfect-debounce (ESM-only package).
// Used by @vue/devtools-kit which is a transitive dependency of vue-router 5.
// In the test environment devtools are not active, so a passthrough is sufficient.
exports.debounce = (fn) => fn;
